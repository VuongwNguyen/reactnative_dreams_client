import {StyleSheet} from 'react-native';
import { Colors } from '../app/Colors';
const PolicyPrivacyStyle = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Colors.background,
    paddingHorizontal: 10,
  },
});
export default PolicyPrivacyStyle;
